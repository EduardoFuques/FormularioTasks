<nav class="navbar navbar-dark bg-success">
  <div class="container-fluid">

    <a class="navbar-brand" href="https://www.iaavim.gob.ar/"><img
        src="https://iaavim.unam.edu.ar/images/imagenes/nuevo_logo_blanco_gg.png"
        alt="Logo"
        width="556"
        height="80"
        class="d-inline-block align-text-top"
      />
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="offcanvas"
      data-bs-target="#offcanvasDarkNavbar"
      aria-controls="offcanvasDarkNavbar"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div
      class="offcanvas offcanvas-end text-bg-success"
      tabindex="-1"
      id="offcanvasDarkNavbar"
      aria-labelledby="offcanvasDarkNavbarLabel"
    >
      <div class="offcanvas-header">
        <h5
          class="offcanvas-title"
          id="offcanvasDarkNavbarLabel"
        >Operaciones</h5>
        <button
          type="button"
          class="btn-close btn-close-white"
          data-bs-dismiss="offcanvas"
          aria-label="Close"
        ></button>
      </div>
      <div class="offcanvas-body">
        <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
          <li class="nav-item">
            {{#if user}}
              <a
                class="nav-link active"
                aria-current="page"
                href="/form"
              >Inicio</a>
            {{/if}}
            {{#unless user}}
              <a class="nav-link active" aria-current="page" href="/">Inicio</a>
            {{/unless}}
          </li>
          {{#unless user}}
            <li class="nav-item">
              <a
                class="nav-link active"
                aria-current="page"
                href="/signup"
              >Registrarse</a>
            </li>
          {{/unless}}
          {{#if user}}
            {{#if editar}}
              <li class="dropdown-item">
                <a class="nav-link" href="/pdf">Descargar formulario de registro</a>
              </li>
            {{/if}}

          {{/if}}
          <li>
            <hr class="" />
          </li>
          <li><a class="dropdown-item" href="https://www.iaavim.gob.ar">Conocer
              mas del IAAviM</a></li>
          {{#if user}}
            <li>
              <hr class="" />
            </li>
            <li><a class="dropdown-item" href="/logout">Salir</a></li>
          {{/if}}
        </ul>
      </div>
    </div>
  </div>
</nav>